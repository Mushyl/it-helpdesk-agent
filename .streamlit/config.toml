# Project-level Streamlit configuration (committed to the repo).
# Gives the web UI a consistent, professional look across machines.

[theme]
primaryColor = "#2563EB"
backgroundColor = "#FFFFFF"
secondaryBackgroundColor = "#F1F5F9"
textColor = "#0F172A"

[browser]
gatherUsageStats = false
